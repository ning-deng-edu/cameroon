1.Non-elegant checkPersonIntro and checkPersonAns, can't send arguments in the showAlert API
2.Add a new intro session list for every person, then call the loadSessionInfo("answer") to load the session info
